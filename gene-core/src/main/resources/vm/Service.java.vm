package ${package}.service;

import java.util.List;

import ${package}.entity.${className}Entity;

/**
 * ${comments}
 *
 * @author ${author}
 * @date ${datetime}
 */
public interface ${className}Service {

    /**
     * 插入一条记录
     * @param ${classname}Entity
     * @return
     */
    int insert(${className}Entity ${classname}Entity);

    /**
     * 批量插入记录
     * @param list
     * @return
     */
    int insertBatch(List<${className}Entity> list);

    /**
     * 根据主键更新记录
     * @param ${classname}Entity
     * @return
     */
    int update(${className}Entity ${classname}Entity);

    /**
     * 根据主键删除记录
     * @param id
     * @return
     */
    int delete(Object id);

    /**
     * 根据主键批量删除记录
     * @param ids
     * @return
     */
    int deleteBatch(Object[] ids);

    /**
     * 根据主键查询记录
     * @param id
     * @return
     */
        ${className}Entity query(Object id);

    /**
     * 根据entity查询记录列表
     * @param ${classname}Entity
     * @return
     */
    List<${className}Entity> queryListByEntity(${className}Entity ${classname}Entity);

    /**
     * 根据entity查询记录总数
     * @param ${classname}Entity
     * @return
     */
    int queryTotal(${className}Entity ${classname}Entity);
}
